---
import { getGlucoseDays } from '@/api/glucose.ts';
import { Chart } from '@/components/Chart.tsx';

const data = await getGlucoseDays(1)
console.log(data)
---
<div>
  <Chart client:load data={data}/>
</div>